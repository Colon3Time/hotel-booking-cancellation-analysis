# Progress Log

## 2026-09-11

### Setup
- สร้างโฟลเดอร์โปรเจกต์ + README พร้อมโจทย์ธุรกิจและ data dictionary
- โหลด dataset จาก TidyTuesday (ชุดเดียวกับ Kaggle) → `data/hotels.csv` (16.9 MB)
- ตรวจแล้ว: 119,390 แถว × 32 คอลัมน์ ตรงกับหน้า Kaggle

### ข้อสังเกตจากการเปิดดูข้อมูลครั้งแรก (ยังไม่ได้ clean)
ตัวเลขด้านล่างเป็นการดูข้อมูลดิบเท่านั้น ยังไม่ใช่ข้อสรุป ต้องตรวจซ้ำหลังทำความสะอาด

**ภาพรวม**
- City Hotel 79,330 แถว · Resort Hotel 40,060 แถว
- วันเข้าพัก ก.ค. 2015 – ส.ค. 2017 (ปี 2015 มีแค่ 6 เดือน, ปี 2017 มี 8 เดือน → เทียบรายปีตรง ๆ ไม่ได้)
- อัตรายกเลิกรวม 37.04% (City Hotel 41.73% · Resort Hotel 27.76%)

**ค่าว่าง**
- `children` 4 แถว · `country` 488 แถว · `agent` 16,340 แถว · `company` 112,593 แถว (ค่าว่างใน agent/company อาจหมายถึง "ไม่ได้จองผ่านบริษัท" ไม่ใช่ข้อมูลหาย)

**จุดที่ต้องตรวจต่อ**
- แถวซ้ำทุกคอลัมน์ 31,994 แถว แต่ข้อมูลไม่มีรหัสการจอง จึงอาจเป็นการจองคนละรายการที่ค่าเหมือนกัน
- `adr` ติดลบ 1 แถว · เท่ากับ 0 จำนวน 1,959 แถว · สูงสุด 5,400 (แถวเดียวที่เกิน 1,000)
- จำนวนแขกรวม (ผู้ใหญ่ + เด็ก + ทารก) = 0 จำนวน 180 แถว
- จำนวนคืนรวม = 0 จำนวน 715 แถว
- `meal` มีค่า Undefined 1,169 แถว (ตามต้นฉบับ Undefined กับ SC มีความหมายเดียวกัน)
- **`deposit_type` = Non Refund มีอัตรายกเลิก 99.4%** (No Deposit 28.4% · Refundable 22.2%) ขัดกับที่คาดว่ามัดจำแบบคืนเงินไม่ได้ควรทำให้ยกเลิกน้อยลง ต้องสืบก่อนสรุปคำถามข้อ 3
- `reservation_status` ตรงกับ `is_canceled` 100% (Canceled 43,017 + No-Show 1,207 = ยกเลิก) → ห้ามใช้อธิบายการยกเลิก

**ข้อมูลหมวดหมู่**
- `distribution_channel`: TA/TO 97,870 · Direct 14,645 · Corporate 6,677 · GDS 193 · Undefined 5
- `market_segment`: Online TA 56,477 · Offline TA/TO 24,219 · Groups 19,811 · Direct 12,606 · Corporate 5,295 · Complementary 743 · Aviation 237 · Undefined 2
- `country`: 177 ประเทศ · มากสุด PRT 48,590 · GBR 12,129 · FRA 10,415 · ESP 8,568 · DEU 7,287
- `lead_time`: มัธยฐาน 69 วัน · เฉลี่ย 104 วัน · สูงสุด 737 วัน

---

### ตัดสินใจเรื่องค่าว่าง (ยังไม่ได้เขียนโค้ด)

**เกณฑ์:** ค่าว่างไม่เกิน 5% **ต่อคอลัมน์** → ลบแถวได้
- ที่มา: Schafer (1999) ระบุว่าข้อมูลหาย ≤ 5% มักไม่มีผลต่อการวิเคราะห์ (อ้างใน Dong & Peng, 2013)
- เงื่อนไข: เกณฑ์นี้ปลอดภัยที่สุดเมื่อข้อมูลหายแบบสุ่ม (MCAR ตาม Rubin, 1976) ถ้าหายไม่สุ่ม ต้องตรวจผลกระทบจริงก่อนลบ
- **ห้ามใช้เกณฑ์กับทั้งตาราง:** ค่าว่างรวม 129,425 ช่อง = 3.39% ของ 3,820,480 ช่อง ดูเหมือนผ่านเกณฑ์ แต่แถวที่มีค่าว่างอย่างน้อย 1 ช่องมี 119,173 แถว (99.82%) ถ้าใช้ `df.dropna()` ทั้งตารางจะเหลือ 217 แถว (สาเหตุหลักคือ `company`)

**การตัดสินใจรายคอลัมน์**

| คอลัมน์ | % ว่าง | วิธีจัดการ | เหตุผล |
|---|---|---|---|
| `company` | 94.31% | ลบทั้งคอลัมน์ | ไม่มีคำถามธุรกิจข้อใดใช้ · ข้อมูลการจองในนามบริษัทมีใน `market_segment = Corporate` แล้ว |
| `children` | 0.0034% (4 แถว) | ลบแถว แล้วแปลงเป็นจำนวนเต็ม | ผ่านเกณฑ์ 5% · คอลัมน์เป็นทศนิยมเพราะมี NaN |
| `country` | 0.41% (488 แถว) | ลบแถว | ผ่านเกณฑ์ 5% · หายไม่สุ่ม (419 แถวเป็น Resort Hotel ที่ไม่ยกเลิก, แถวที่ว่างยกเลิกเพียง 13.7%) แต่ตรวจแล้วกระทบน้อย |
| `agent` | 13.69% | แทนด้วย 0 = ไม่ผ่านบริษัทนำเที่ยว | ไม่ใช่ข้อมูลหาย แต่เป็นค่าที่ไม่มีอยู่จริง (ส่วนใหญ่เป็น Direct/Corporate/Groups) · รหัส 0 ไม่ถูกใช้ในข้อมูล · **ห้ามลบแถว** เพราะไม่มี agent ยกเลิก 24.7% เทียบกับมี agent 39% |

**ผลกระทบของการลบแถว `children` + `country`** (ทดลองคำนวณแล้ว)
- ลบ 492 แถว → เหลือ **118,898 แถว**
- อัตรายกเลิกรวม 37.04% → 37.14% · City Hotel 41.73% → 41.71% · Resort Hotel 27.76% → 27.98%
- ทุกค่าเปลี่ยนไม่ถึง 0.3 จุด ไม่กระทบข้อสรุป

**ลำดับที่จะทำในโค้ด:** ลบคอลัมน์ `company` → ลบแถวที่ `children` หรือ `country` ว่าง (`dropna(subset=...)`) → แทนค่าว่าง `agent` ด้วย 0 → แปลง `children` เป็นจำนวนเต็ม
- **ตรวจผล:** `len(df)` ต้องได้ 118,898 และ `df.isna().sum()` ต้องเป็น 0 ทุกคอลัมน์

**ค่าว่างแฝง (ไม่ใช่ NaN, ไม่เร่งด่วน)**
- `meal` = Undefined 1,169 แถว → เปลี่ยนเป็น SC (ต้นฉบับระบุว่าความหมายเดียวกัน)
- `distribution_channel` = Undefined 5 แถว · `market_segment` = Undefined 2 แถว → คงไว้

### เกณฑ์แบ่งกลุ่มคอลัมน์ (ร่างแรก ปรับได้ระหว่างทาง)

ตัวตัดสินคือคำถามธุรกิจ 6 ข้อใน README ถามทีละคอลัมน์ตามลำดับ
- **Q1:** ต้องใช้คอลัมน์นี้ "คำนวณ" คำตอบข้อใดข้อหนึ่งหรือไม่ → ใช้ = **กลุ่ม A**
- **Q2:** ถ้าไม่ตรวจหรือไม่จัดการคอลัมน์นี้ คำตอบจากกลุ่ม A อาจผิดหรือไม่ → อาจผิด = **กลุ่ม B** · ไม่มีผล = **กลุ่ม C**
- ที่มา: เกณฑ์ relevance + quality ของงาน Select Data ใน CRISP-DM · คิดจากเป้าหมายลงมาตาม GQM (Basili)
- คอลัมน์มีปัญหา ≠ อยู่กลุ่ม B อัตโนมัติ ต้องดูว่าปัญหานั้นกระทบคำตอบหรือไม่ (เช่น `meal`)

**กลุ่ม A — ใช้ตอบคำถาม (12)** → ทำความสะอาดละเอียด
`is_canceled` (ทุกข้อ) · `hotel`, `distribution_channel`, `market_segment` (ข้อ 1) · `lead_time` (ข้อ 2) · `deposit_type` (ข้อ 3) · `adr`, `stays_in_weekend_nights`, `stays_in_week_nights`, `arrival_date_year`, `arrival_date_month` (ข้อ 4–5) · `is_repeated_guest` (ข้อ 6)

**กลุ่ม B — ใช้ตรวจความถูกต้อง (7)** → แก้เฉพาะจุด
- `adults`, `children`, `babies` → ตรวจว่า `adr = 0` มาจากแถวที่ไม่มีแขกหรือไม่
- `reservation_status` → ตรวจ leakage ของ `is_canceled`
- `agent`, `company` → ตรวจความสอดคล้องกับ `distribution_channel`
- `country` → การลบแถวที่ว่างกระทบอัตรายกเลิก

**กลุ่ม C — ยังไม่ได้ใช้ (13)** → ดูผ่าน ๆ ด้วย `describe()`
`arrival_date_week_number`, `arrival_date_day_of_month`, `previous_cancellations`, `previous_bookings_not_canceled`, `reserved_room_type`, `assigned_room_type`, `booking_changes`, `days_in_waiting_list`, `customer_type`, `required_car_parking_spaces`, `total_of_special_requests`, `reservation_status_date`, `meal`
- `previous_cancellations`, `customer_type` อาจย้ายไปกลุ่ม A ในขั้น EDA

### งานค้างรอบถัดไป
1. เขียนโค้ดจัดการค่าว่างใน `data_cleaning.py` ตามตารางข้างบน แล้วตรวจผลตามเงื่อนไข
2. ทำความเข้าใจคอลัมน์กลุ่ม A ด้วย `value_counts()` / `describe()` เทียบกับ Data Dictionary ใน README (รายละเอียดเพิ่มดูบทความต้นฉบับ Antonio et al., 2019)
3. คืนค่า `data/.gitkeep` ด้วย `git checkout -- data/.gitkeep` (มีโค้ดทดลองค้างอยู่)
4. ขั้นถัดไปหลังค่าว่าง: `adr` ติดลบ/เท่ากับ 0 · แขกรวม 0 · คืนรวม 0 · แถวซ้ำ
